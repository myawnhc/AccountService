hazelcast-client:
    cluster-name: acctsvc
#    properties:
    network:
      cluster-members:
      - localhost:5701
#    properties:
#      hazelcast.config.schema.validation.enabled: false
#    serialization:
#      compact-serialization:
#        enabled: true